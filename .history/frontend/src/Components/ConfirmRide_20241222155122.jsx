import React from 'react'

const ConfirmRide = ({confirmRide,setConfirmRide}) => {
  return (
    <div className={`${confirmRide ? '' : 'hidden'} bottom-0 absolute bg-white w-full`}>
      hi
    </div>
  )
}

export default ConfirmRide
